<section class="top-section {{type}}">
    <div class="top-head">
        <h2>top {{type}}</h2>
        <button class="tab selected-tr-btn" value="short_term" onclick="c(this)">last month</button>
        <button class="tab" value="medium_term" onclick="c(this)">last 6 month</button>
        <button class="tab" value="long_term" onclick="c(this)">all time</button>
    </div>
    {{#each top}}
    <ol class="top-list {{time_range}} {{#if @index}}{{else}}selected-tr{{/if}}">
        {{#each names}} 
            <li>
                {{#if ../urls}}<a href="{{lookup ../urls  @index }}" target="_blank">{{/if}}
                    {{this}} 
                    {{#if ../paired}}<span>{{lookup ../paired @index}}</span>{{/if}}
                {{#if ../urls}}</a>{{/if}}
            </li>
        {{/each}}
    </ol>
    {{/each}}
</section>
